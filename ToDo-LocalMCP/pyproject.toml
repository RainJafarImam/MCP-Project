[project]
name = "todo-mcp-server"
version = "0.1.0"
description = "A local MCP server for managing to-do tasks"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.12.3",
]
